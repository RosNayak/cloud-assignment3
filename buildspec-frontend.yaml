version: 0.2

phases:
  build:
    commands:
      - echo "Preparing frontend artifact"
      - mkdir -p build
      - cp -r frontend/* build/
artifacts:
  files:
    - build/**/*
  discard-paths: no
